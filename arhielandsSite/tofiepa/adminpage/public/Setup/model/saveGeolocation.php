<?php
ob_start();
include '../../../config.php';
include SPATH_LIBRARIES . DS . "engine.Class.php";

$engine = new engineClass();

if (!empty($latitude) && !empty($longitude)) {

        //Post goes here
}
